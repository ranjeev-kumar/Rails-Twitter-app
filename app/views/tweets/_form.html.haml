= form_for @tweet do |f|
  .form-group
    - if @tweet.errors.any?
      #error_explanation
        %h3
          = pluralize(@tweet.errors.count, "error")
          Prohibited this tweet form being saved:
        %ul
          - @tweet.errors.full_messages.each do |msg|
            %li= msg
  .form-group
    = f.text_area :body, class: 'form-control', placeholder:'Tweet Here...'
  .form-group
    = f.submit :submit, class: 'btn btn-default'